""""""""""""""""
" SETTINGS
""""""""""""""""

" vim: filetype=vifm :
" Sample configuration file for vifm (last updated: 27 Jun, 2013)
" You can edit this file by hand.
" The " character at the beginning of a line comments out the line.
" Blank lines are ignored.
" The basic format for each item is shown with an example.

" This is the actual command used to start vi.  The default is vim.
" If you would like to use another vi clone such Elvis or Vile
" you will need to change this setting.
set vicmd=vim

" Trash Directory
" The default is to move files that are deleted with dd or :d to
" the trash directory.  If you change this you will not be able to move
" files by deleting them and then using p to put the file in the new location.
" I recommend not changing this until you are familiar with vifm.
" This probably shouldn't be an option.
set trash

" This is how many directories to store in the directory history.
set history=512

" Follow links on l or Enter.
set followlinks

" With this option turned on you can run partially entered commands with
" unambiguous beginning using :! (e.g. :!Te instead of :!Terminal or :!Te<tab>).
set fastrun

" Natural sort of (version) numbers within text.
set sortnumbers

" Maximum number of changes that can be undone.
set undolevels=1024

" If you installed the vim.txt help file set vimhelp.
" If would rather use a plain text help file set novimhelp.
set novimhelp

" If you would like to run an executable file when you
" press return on the file name set this.
set norunexec

" More readible units
set noiec

" The FUSE_HOME directory will be used as a root dir for all FUSE mounts.
" Unless it exists with write/exec permissions set, vifm will attempt to
" create it.
set fusehome=/tmp/vifm_FUSE

" Format for displaying time in file list. For example:
" TIME_STAMP_FORMAT=%m/%d-%H:%M
" See man date or man strftime for details.
set timefmt=%Y-%m-%d\ %H:%M

" Show list of matches on tab complition in command-line mode
set wildmenu

" Ignore case in search patterns unless it contains at least one uppercase letter
set ignorecase
set smartcase

" Highlight search results automatically
set hlsearch

" Use increment searching (search while typing)
set incsearch

" Try to leave some space from cursor to upper/lower border in lists
set scrolloff=4

" Don't do to much requests to slow file systems
set slowfs=curlftpfs

" Set custom status line look
set statusline="  %t%= %A %10u:%-7g %15s %20d  "

" Integrate with tmux
screen

" What should be saved automatically between vifm runs
" Like in previous versions of vifm
" set vifminfo=options,filetypes,commands,bookmarks,dhistory,state,cs
" Like in vi
set vifminfo=dhistory,savedirs,chistory,state,tui,shistory,phistory,fhistory,dirstack,registers,bookmarks


""""""""""""""""
" THEME
""""""""""""""""

" Selected color scheme
" colorscheme Default
colorscheme solarized-dark


""""""""""""""""
" MARKS
" :mark mark /full/directory/path [filename]
""""""""""""""""

" home
mark h ~
mark p ~/private
" downloads
mark d ~/downloads
mark b ~/downloads/browser
mark s ~/downloads/stream
" dev
mark t ~/dotfiles
mark e ~/dev
mark z ~/dev/zipscene
" media
mark m /media/main
mark v /media/main/video
" apps
mark D ~/Dropbox


""""""""""""""""
" COMMANDS
""""""""""""""""

" :com[mand] command_name action
" The following macros can be used in a command
" %a is replaced with the user arguments.
" %c the current file under the cursor.
" %C the current file under the cursor in the other directory.
" %f the current selected file, or files.
" %F the current selected file, or files in the other directory.
" %b same as %f %F.
" %d the current directory name.
" %D the other window directory name.
" %m run the command in a menu window

command df df -h %m 2> /dev/null
command diff vim -d %f %F
command zip zip -r %f.zip %f
command run !! ./%f
command make !!make
command vgrep vim "+grep %a"


""""""""""""""""
" MAPPINGS
""""""""""""""""

" MISC

nmap ; :
nmap s :shell<cr>

" sort
nmap S :sort<cr>

" refresh view
nmap ,r R

" reload config
nmap ,,r :source ~/.vifm/vifmrc<cr>

" opening console in current directory
nmap ,t :!i3-sensible-terminal &<cr>

" quit
nmap q :q<cr>
nmap ,w :q<cr>

" fast renaming shortcuts
nmap I cw<c-a>
nmap cc cw<c-u>
nmap A cw<c-w>


" PANES

unmap <space>

" switch panes
nmap ,b <c-w>w
" navigate panes
nmap j <c-w>j
nmap k <c-w>k
nmap h <c-w>h
nmap l <c-w>l
" split panes
nmap <space>bj :split<cr><c-w>j
nmap <space>bk :split<cr><c-w>k
nmap <space>bh :vsplit<cr><c-w>h
nmap <space>bl :vsplit<cr><c-w>l
" show only one pane
nmap <space>bo :only<cr>


" FILES

" open-with menu
nmap ,f :file<cr>
vmap ,f :file<cr>

" preview
nmap ,p :!gloobus-preview %c:p<cr>

" vifm.vim intergation
nmap oc :EditVim<cr>
nmap ot :TabVim<cr>
nmap o- :SplitVim<cr>
nmap oj :SplitVim<cr>
nmap ok :SplitVim<cr>
nmap os :VsplitVim<cr>
nmap ol :VsplitVim<cr>
nmap oh :VsplitVim<cr>


" TOGGLES

" show file previews
nmap cop :view<cr>
" toggle wrap
nmap cow :set wrap!<cr>


" CLIPBOARD

" copy selected file path
nmap yp :!echo -n %c:p | xsel -ib<cr>
" copy selected filename
nmap yf :!echo -n %c | xsel -ib<cr>
" copy current directory
nmap yd :!echo -n %d | xsel -ib<cr>


" TO BE VETTED

" opening vim to edit .vifmrc
nmap ,c :execute ':!vim $MYVIFMRC' | restart<cr>
" opening gvim to edit .vifmrc
nmap ,C :!gvim --remote-tab-silent $MYVIFMRC &<cr>

" open file in the background using its default program
nmap gb :file &<cr>l

" standard two-panel file managers mappings
nmap <f3> :!less %f<cr>
nmap <f4> :edit<cr>
nmap <f5> :copy<cr>
nmap <f6> :move<cr>
nmap <f7> :mkdir<space>
nmap <f8> :delete<cr>


""""""""""""""""
" FILETYPES
""""""""""""""""

" DIRECTORIES

filextype */
	 \ {SpaceFM}
		 \ spacefm %f %i &,
fileviewer .*/,*/
	\ ls -sAh %f


" MEDIA

" audio
filetype *.wav,*.mp3,*.flac,*.ogg,*.m4a
	\ {Play using ffplay}
		\ ffplay,
	\ {Play using MPlayer}
		\ mplayer %f,
fileviewer *.mp3
	\ mp3info

" videos
filextype *.avi,*.mp4,*.wmv,*.dat,*.3gp,*.ogv,*.mkv,*.mpg,*.vob,*.flv
	 \ {VLC}
		 \ vlc %c %i &,
	 \ {ffplay}
		 \ ffplay -fs,
fileviewer *.avi,*.mp4,*.wmv,*.dat,*.3gp,*.ogv,*.mkv,*.mpg,*.vob
	  \ vlc
" fileviewer *.avi,*.mp4,*.wmv,*.dat,*.3gp,*.ogv,*.mkv,*.mpg,*.vob
	  " \ ffprobe -pretty %c 2>&1

" images
filextype *.bmp,*.jpg,*.jpeg,*.png,*.gif,*.xpm
	 \ {geeqie}
		 \ geeqie
	 \ {feh}
		 \ feh -FZ %d --start-at %d/%c 2>/dev/null
fileviewer *.bmp,*.jpg,*.jpeg,*.png,*.gif,*.xpm
	\ convert -identify %f -verbose /dev/null

" web
filextype *.html,*.htm
	\ chrome,
	\ google-chrome,
	\ google-chrome-unstable,
	\ firefox,
filetype *.html,*.htm
	\ links,


" DOCS

" general
filextype *.doc,*.docx,*.xls,*.xlsx
	\ {LibreOffice}
		\ libreoffice %f %i &
fileviewer *.docx,
	\ docx2txt.pl %f -

" PDFs
filetype *.pdf
	\ zathura %f,
	\ apvlv %f,
	\ xpdf %f
fileviewer *.pdf
	\ pdftotext -nopgbrk %c -


" ARCHIVES

fileviewer *.tgz,*.tar.gz
	\ tar -tzf
fileviewer *.tar.bz2,*.tbz2
	\ tar -tjf

filetype *.rar
	\ {extract}
		\ unrar x,

filetype *.zip,*.jar,*.war,*.ear
	\ {view contents}
		\ zip -sf %c | less,
		\ tar -xf,
fileviewer *.zip,*.jar,*.war,*.ear
	\ zip -sf
" fileviewer *.zip,*.jar,*.war,*.ear
" 	\ unzip -l

" MISC

" MD5
filetype *.md5
	\ {Check MD5 hash sum}
		\ md5sum -c %f,


" DEFAULTS

filetype *
	\ xdg-open
